#include <bits/stdc++.h>
using namespace std;

#define int long long
#define fastio ios::sync_with_stdio(false); cin.tie(nullptr);

void solve(int i , int n, vector<int> &v , int sm , int target , vector<int> res) {
   
      vector<vector<int>> ans;


}

void combination(){
     int n;
    cin >> n;
    vector<int> v(n);
    int target;
    cin >> target;
    for(int i = 0;i < n ; i++){
        cin >> v[i];
    }
}

int32_t main() {
    fastio;
    int t = 1;
    cin >> t;
    while (t--) solve();
    return 0;
}